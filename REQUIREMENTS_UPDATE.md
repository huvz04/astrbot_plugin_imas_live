# 2026-09-07 用户最新需求（覆盖初版群交互设计）

用户已完成需求补充，并明确要求开发任务现在开始执行。请立即按本文件和下列完整修订方案继续实现，完成图片预览、提醒逻辑测试、README 和配置更新后交付，不要仅回复准备就绪或再次要求确认已授权的本地开发。

群命令只保留 `/imaslive`，直接返回图片日历。自动消息只保留现场抽票截止前一次图片提醒，默认提前 60 分钟，提前量可在 WebUI 配置。群白名单、企划筛选等也放 WebUI，不新增群子命令。

最新视觉约束：`/imaslive` 自动展示从今天起的未来 30 个自然日，采用按日期分组的纵向列表图片。六家分别使用既有生日插件品牌色，合同 live 统一粉色并显示合同/参与企划标签。具体色值、合同判定及跨月/远期演出截止条目的处理见下述修订文档。此要求替代原来的按周/月格日历建议，不新增自动日报。

开发须阅读共享方案的补充：

- `../research/imas-live/calendar-and-deadline-revision.md`
- `../research/imas-live/feasibility-and-development-plan.md`

这些是尚需落实的用户修订，不代表现有代码已经实现。请相应收敛原有命令和推送类型，新增确定性的图片日历/提醒渲染、独立于网络同步的本地定时检查、按群与轮次/截止时间持久去重，并验证预览图片、可配置提前量和重启行为。评估任务未改动开发中的 Python 文件。
